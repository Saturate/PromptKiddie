# PromptKiddie workspace configuration
# Copy to .pk/config.toml and adjust. Values here override ~/.pk/config.toml.
# Environment variables (DATABASE_URL, PK_*) override both.

[database]
url = "postgres://promptkiddie:changeme_local_only@localhost:5432/promptkiddie"

[toolbox]
container = "pk-toolbox"             # Docker container name (pk-agent image)
# host = "ssh://user@remote"         # remote toolbox (future)
timeout = 300000                      # command timeout in ms
exec_mode = "docker"                  # "docker" or "local"

[workspace]
evidence_root = "./engagements"
tool_log_dir = "./engagements/.tool-log"

[vpn]
config_path = "./vpn"                 # mounted to /vpn in the toolbox

[inbox]
poll_seconds = 15

[api]
# url = "http://localhost:3200"      # API server URL (when set, CLI/MCP use HTTP instead of direct DB)
# secret = "your-secret-here"        # bearer token for API auth
port = 3200                           # port for the API server to listen on
